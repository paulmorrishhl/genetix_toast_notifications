name: genetix_toast_notifications
description: "A custom Flutter package for displaying simple styled toast notifications with predefined duration settings."
version: 0.1.0
repository: https://github.com/paulmorrishhl/genetix_toast_notifications
authors:
  - Paul Morris <paulmorrishhl@gmail.com>

environment:
  sdk: '>=3.3.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

